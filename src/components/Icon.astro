---
interface Props {
  name: string;
  class?: string;
}

const { name, class: className = "" } = Astro.props;
---

<svg class:list={["site-icon", className]} aria-hidden="true">
  <use href={`/assets/images/icons-v20260706.svg#icon-${name}`}></use>
</svg>
